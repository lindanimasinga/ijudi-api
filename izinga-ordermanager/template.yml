# This AWS SAM template has been generated from your function's 
# configuration. If your function has one or more triggers, note 
# that the AWS resources associated with these triggers aren't fully 
# specified in this template and include placeholder values.Open this template 
# in AWS Application Composer or your favorite IDE and modify 
# it to specify a serverless application with other AWS resources. 
AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: An AWS Serverless Specification template describing your function.
Resources:
  izingaordermanager:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ./izinga-ordermanager
      Description: ''
      MemorySize: 2048
      Timeout: 60
      Handler: io.curiousoft.izinga.ordermanagement.StreamLambdaHandler::handleRequest
      Runtime: java17
      Architectures:
        - x86_64
      EventInvokeConfig:
        MaximumEventAgeInSeconds: 21600
        MaximumRetryAttempts: 2
      EphemeralStorage:
        Size: 512
      Events:
        Api1:
          Type: Api
          Properties:
            Path: /MyResource
            Method: ANY
        Api2:
          Type: Api
          Properties:
            Path: /{proxy+}
            Method: ANY
        Api3:
          Type: Api
          Properties:
            Path: /
            Method: ANY
        Api4:
          Type: Api
          Properties:
            Path: /MyResource
            Method: ANY
        Schedule1:
          Type: Schedule
          Properties:
            Schedule: cron(0/14 6-16 ? * MON-SAT *)
      Environment:
        Variables:
          JDK_JAVA_OPTIONS: >-
            --add-opens java.base/java.util=ALL-UNNAMED --add-opens
            java.base/java.lang=ALL-UNNAMED
          SPRING_PROFILES_ACTIVE: uat
      RuntimeManagementConfig:
        UpdateRuntimeOn: Auto
      SnapStart:
        ApplyOn: None
      PackageType: Zip
      Policies:
        Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
            Resource: arn:aws:logs:af-south-1:316005840972:*
          - Effect: Allow
            Action:
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource:
              - >-
                arn:aws:logs:af-south-1:316005840972:log-group:/aws/lambda/izinga-order-manager:*
